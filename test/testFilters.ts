import {expect} from 'chai';
import {describe, it} from 'mocha';
import {filterObject, filterObjectArray} from '../src/index';

describe('filter', () => {
	describe('filterObject', () => {
		it('should return resolve promise', (done) => {
			// filterObject(),
		});
	});
	describe('filterObjectArray', () => {
		it('should return resolve promise', (done) => {
			// filterObjectArray()
		});
	});
});
